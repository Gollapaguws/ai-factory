import React from "react";
import Page from "../../../shared/Page";

// Hybrid Workflow Builder: visual + code-first (YAML/SDK)
export default function WorkflowBuilder() {
  return (
    <Page title="Workflow Builder">
      <p>Visual drag-and-drop and code-first (YAML/SDK) workflow creation coming soon.</p>
      {/* TODO: Add visual workflow editor and YAML/SDK editor */}
    </Page>
  );
}
